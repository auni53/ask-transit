var Transit = require('../dist/index.js').handler();
console.log(Transit);
debugger;
// var ttcClient = new Client('ttc');

// const stops = new Set(Object.keys(data.stops).map(e => data.stops[e]));
// stops.forEach(e => console.log(e));
